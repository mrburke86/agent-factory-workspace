# @acme/evals

Kernel template package for **eval gates**:
- Deterministic smoke suite for PR gating
- Adapter layer supports fixture-mode (default) or live endpoints later
- Full suites are added incrementally as the product grows
