# @acme/contracts

Kernel template package for **versioned contracts**:
- Zod schemas as source of truth
- Standard error & event envelopes
- Breaking-change check via snapshot comparison (v1)

This package is intentionally minimal and stable.
