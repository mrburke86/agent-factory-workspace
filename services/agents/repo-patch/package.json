{
  "name": "@acme/agent-repo-patch",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@acme/contracts": "workspace:*",
    "@acme/agent-runtime": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
